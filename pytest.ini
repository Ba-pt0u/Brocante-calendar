[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v -m "not live"

markers =
    unit: Pure unit tests — no network, no disk I/O
    integration: Mocked HTTP or disk I/O
    live: Real HTTP requests to external sites (run with: pytest -m live)
